# Normalise to LF in the repo. Without this, editing on Windows commits CRLF
# and anything read as a shell script or compared byte-for-byte breaks on
# Linux — which is where this actually runs.
* text=auto eol=lf

*.png binary
*.jpg binary
*.mp4 binary
*.gif binary
